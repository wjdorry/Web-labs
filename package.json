{
  "name": "lab-7-json-server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "serve": "json-server --watch db.json --port 3001"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
